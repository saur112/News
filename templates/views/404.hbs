<!DOCTYPE html>
<html lang="en">
  <head>
{{> headerlink}}
</head>
  <body>
  {{> navbar}}

    <div class="container">
        <h1>{{error}}</h1>
        <h2 class="value">Opps Page not Found</h2>
        <a class="anchor" href="/"><button class="btn">Click me</button></a>
    </div>

    {{> footer}}
    </body>
    </html>